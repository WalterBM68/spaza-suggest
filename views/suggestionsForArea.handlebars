
<div class="container">
    <div class="main_paragraph">
        <form action="/suggestionsForArea" method="post">
            <table>
                <tr>
                    <th>The suggestions for this erea</th>
                </tr>
                {{#each spazaSugestions}}
                <tr>
                    <td>{{product_name}} </td>
                </tr>
                {{/each}}
            </table>
            <button class="submitBtn" type="submit"> Accept suggestion </button>
        </form>
    </div>
</div>